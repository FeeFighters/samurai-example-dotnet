@model SamuraiStore.Models.Order

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Order</legend>

    <div class="display-label">Thing</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Thing.Name)
    </div>

    <div class="display-label">CreatedAt</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CreatedAt)
    </div>

    <div class="display-label">TransactionRef</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.TransactionRef)
    </div>

    <div class="display-label">CreditRef</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.CreditRef)
    </div>

    <div class="display-label">IsCredited</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.IsCredited)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
